{
  "name": "just-enough-claude-code",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal, testable Claude Code harness for small projects — 4 hooks, 3 agents, 3 commands, 1 skill, one install path.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  }
}
